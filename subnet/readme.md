# Test Subnet

This folder maintains a subnet of test unregistered nodes, used to test committee sync against before deploying all components to the live network.

## Contents

- `nodes.json` — describes node names and IPs in the test subnet
- `status.html` — (coming soon) an HTML/JS page showing the current status of the subnet